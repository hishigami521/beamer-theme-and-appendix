\centering
\begin{tikzpicture}[scale=.4]
  \pgfmathsetmacro{\ix}{.5}
  \begin{scope}
    \foreach \x in { 0,...,7 }
    \foreach \y in { -4,...,3 }
    \draw[fill=blue] (\x*\ix,\y*\ix) rectangle +(\ix,\ix);
    \draw (-0.75,-1.25) node{$A$};
  \end{scope}
  
  \begin{scope}[shift={(0:4)}]
      \draw [->,thick,line width=2pt] (0,0) -- (2,0);
  \end{scope}

  \begin{scope}[shift={(0:6)}]
    \draw[fill=white] (0,-2) rectangle +(4,4);
    \foreach \x in { 0,...,7 }
    \draw[fill=darkgreen] (\x*\ix,2-\ix-\x*\ix) rectangle +(\ix,\ix);
    \foreach \x in { 0,...,6 }
    \draw[fill=darkgreen] (\x*\ix+\ix,2-\ix-\x*\ix) rectangle +(\ix,\ix);
    \foreach \x in { 0,...,5 }
    \draw[fill=darkgreen] (\x*\ix+\ix+\ix,2-\ix-\x*\ix) rectangle +(\ix,\ix);
    \foreach \x in { 0,...,6 }
    \draw[fill=darkgreen] (\x*\ix,2-2*\ix-\x*\ix) rectangle +(\ix,\ix);
    \foreach \x in { 0,...,5 }
    \draw[fill=darkgreen] (\x*\ix,2-3*\ix-\x*\ix) rectangle +(\ix,\ix);
    \draw (0.75,-1.25) node{$B$};
  \end{scope}

  \begin{scope}[shift={(0:10)}]
    \draw [->,thick,line width=2pt] (0,0) -- (1,0)
    node[right,align=center, text width=3cm,rounded corners,draw=darkgreen,fill=white,inner sep=1ex]{Compute \\ Eigenpairs of $B$} -- (11,0) 
    node[right,align=center]{$\left(\lambda_j,\bm{q}_j\right)$};
  \end{scope}
\end{tikzpicture}
